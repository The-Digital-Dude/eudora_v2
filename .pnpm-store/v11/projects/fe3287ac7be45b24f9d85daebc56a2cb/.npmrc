# pnpm build script allowlist is configured in pnpm-workspace.yaml

# Disable supply-chain minimum release age check so Docker builds pass
# even when the lockfile references very recently published packages
audit-config-minimum-release-age=0
